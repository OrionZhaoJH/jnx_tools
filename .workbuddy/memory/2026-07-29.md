# 2026-07-29 工作日志

## jnx_tools 拆分重构

- 将 `jnx_tools.html` 拆分为两个独立页面：
  - `adc_analysis.html`：ADC 数据分析工具（日志离线分析，C/B/T/Diff 折线图与统计）
  - `ble_analysis.html`：BLE 分析工具（Web Bluetooth 实时采集）
- 将 `index.html` 从跳转页面改造为工具箱首页，采用数据驱动卡片模式（`TOOLS` 数组配置），后续新增工具只需在数组中添加配置对象
- 每个工具页面添加了「← 返回首页」导航链接
- 删除了旧的 `jnx_tools.html`
- 更新了 `README.md`，包含工具列表表格和新增工具的说明
- GitHub Actions 工作流（`deploy-pages.yml`）无需修改，已使用 `path: .` 部署整个仓库
